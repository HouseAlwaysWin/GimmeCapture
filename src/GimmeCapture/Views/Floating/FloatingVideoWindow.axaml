<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cv="using:GimmeCapture.Converters"
        xmlns:vm="using:GimmeCapture.ViewModels.Floating"
        xmlns:mainVm="using:GimmeCapture.ViewModels.Main"
        xmlns:services="using:GimmeCapture.Services.Core.Infrastructure"
        xmlns:controls="using:GimmeCapture.Views.Controls"
        xmlns:models="using:GimmeCapture.Models"
        xmlns:media="using:Avalonia.Media"
        mc:Ignorable="d" d:DesignWidth="300" d:DesignHeight="300"
        x:Class="GimmeCapture.Views.Floating.FloatingVideoWindow"
        x:DataType="vm:FloatingVideoViewModel"
        Title="Pinned Video"
        WindowDecorations="None"
        Topmost="True"
        Background="Transparent"
        FontFamily="{Binding CurrentFontFamily, Source={x:Static services:LocalizationService.Instance}}"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        ClipToBounds="False">
    <Window.Resources>
        <cv:StringToKeyGestureConverter x:Key="StringToKeyGestureConverter"/>
    </Window.Resources>
        
    <Window.KeyBindings>
        <!-- Drawing Tools -->
        <KeyBinding Gesture="R" Command="{Binding SelectToolCommand}" CommandParameter="{x:Static models:AnnotationType.Rectangle}" />
        <KeyBinding Gesture="E" Command="{Binding SelectToolCommand}" CommandParameter="{x:Static models:AnnotationType.Ellipse}" />
        <KeyBinding Gesture="A" Command="{Binding SelectToolCommand}" CommandParameter="{x:Static models:AnnotationType.Arrow}" />
        <KeyBinding Gesture="L" Command="{Binding SelectToolCommand}" CommandParameter="{x:Static models:AnnotationType.Line}" />
        <KeyBinding Gesture="P" Command="{Binding ToggleToolGroupCommand}" CommandParameter="Pen" />
        <KeyBinding Gesture="T" Command="{Binding ToggleToolGroupCommand}" CommandParameter="Text" />
        <KeyBinding Gesture="M" Command="{Binding SelectToolCommand}" CommandParameter="{x:Static models:AnnotationType.Mosaic}" />
        <KeyBinding Gesture="B" Command="{Binding SelectToolCommand}" CommandParameter="{x:Static models:AnnotationType.Blur}" />

        <!-- History -->
        <KeyBinding Gesture="Ctrl+Z" Command="{Binding UndoCommand}" />
        <KeyBinding Gesture="Ctrl+Y" Command="{Binding RedoCommand}" />
        <KeyBinding Gesture="Delete" Command="{Binding DeleteSelectedAnnotationCommand}" />

        <!-- Mode Actions -->
        <KeyBinding Gesture="Space" Command="{Binding TogglePlaybackCommand}" />
        <KeyBinding Gesture="Shift+M" Command="{Binding ToggleMuteCommand}" />
        <KeyBinding Gesture="{Binding SelectionHotkey, Converter={StaticResource StringToKeyGestureConverter}}" Command="{Binding SelectionCommand}" />
        <KeyBinding Gesture="{Binding CropHotkey, Converter={StaticResource StringToKeyGestureConverter}}" Command="{Binding CropCommand}" />
        <KeyBinding Gesture="{Binding PinHotkey, Converter={StaticResource StringToKeyGestureConverter}}" Command="{Binding PinSelectionCommand}" />
        <KeyBinding Gesture="F4" Command="{Binding ToggleToolbarCommand}" />

        <!-- Core Actions -->
        <KeyBinding Gesture="Ctrl+S" Command="{Binding SaveCommand}" />
        <KeyBinding Gesture="Ctrl+C" Command="{Binding CopyCommand}" />
        <!-- Esc cancels the in-progress action (handled in code-behind); Ctrl+W closes the window. -->
        <KeyBinding Gesture="Ctrl+W" Command="{Binding CloseCommand}" />
    </Window.KeyBindings>

    <Window.Styles>
        <Style Selector="Grid.Handle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="IsVisible" Value="{Binding !IsAnyToolActive}"/>
        </Style>
        <Style Selector="Border.MainBorder">
            <Setter Property="BoxShadow" Value="{Binding BorderColor, Converter={StaticResource ColorToBoxShadowConverter}}"/>
            <Setter Property="BorderThickness" Value="{Binding BorderThickness, Converter={StaticResource ThicknessConverter}}"/>
        </Style>
        <Style Selector="Border.MainBorder.hidden-border">
            <Setter Property="BoxShadow" Value="0 0 0 0 Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style Selector="Border.InnerBorder.hidden-border">
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Styles>
    
    <Grid Name="RootGrid" Background="Transparent" ClipToBounds="False">

        <!-- BABYMETAL Style Border (Wraps VIDEO ONLY) -->
        <Border x:Name="MainBorder"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Width="{Binding DisplayWidth}"
                Height="{Binding DisplayHeight}"
                Margin="{Binding WindowPadding}"
                Classes="MainBorder"
                Classes.hidden-border="{Binding HidePinBorder}"
                CornerRadius="0"
                ClipToBounds="False"
                Background="{DynamicResource GothicBlackBrush}">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,100%" Opacity="{Binding HidePinBorder, Converter={StaticResource BoolInvertConverter}}">
                    <GradientStop Offset="0" Color="{Binding BorderColor}"/>
                    <GradientStop Offset="0.5" Color="{Binding BorderColor}"/>
                    <GradientStop Offset="1" Color="{Binding BorderColor}"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Border Classes="InnerBorder" ClipToBounds="False">
                <Grid ClipToBounds="False">
                    <!-- Content Alignment Layer -->
                    <Grid ClipToBounds="False">
                        <!-- Video Base -->
                        <Image Source="{Binding VideoBitmap}" 
                               Stretch="Fill" 
                               Name="PinnedVideo"/>
                        
                        <!-- Processing/Export Overlay -->
                        <Border IsVisible="{Binding IsExporting}" 
                                Background="#AA000000" 
                                CornerRadius="4">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="10">
                                <TextBlock Text="Processing Export..." 
                                           HorizontalAlignment="Center" 
                                           Foreground="White" 
                                           FontSize="14" FontWeight="Bold"/>
                                <ProgressBar Value="{Binding ExportProgress}" 
                                             Minimum="0" Maximum="100" 
                                             Width="150" Height="8" 
                                             Foreground="{Binding BorderColor, Converter={StaticResource ColorToBrushConverter}}"/>
                                <TextBlock Text="{Binding ExportProgress, StringFormat={}{0:F1}%}" 
                                           HorizontalAlignment="Center" 
                                           Foreground="#CCCCCC" 
                                           FontSize="10"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Annotation Layer -->
                        <ItemsControl ItemsSource="{Binding Annotations}" IsHitTestVisible="False">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas Width="{Binding DisplayWidth}" Height="{Binding DisplayHeight}"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="models:Annotation">
                                    <controls:AnnotationControl Annotation="{Binding}" 
                                                                Width="{Binding $parent[Window].((vm:FloatingVideoViewModel)DataContext).DisplayWidth}"
                                                                Height="{Binding $parent[Window].((vm:FloatingVideoViewModel)DataContext).DisplayHeight}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                    </Grid>
                    
                    <!-- Decoration Layer (Unified) -->
                    <Grid ClipToBounds="False">
                        <controls:GothicDecoration DecorationBrush="{Binding BorderColor, Converter={StaticResource ColorToBrushConverter}}" 
                                                   Margin="{Binding SelectionBorderOuterMargin}"
                                                   ShowWings="{Binding !HidePinDecoration}"
                                                   ShowCorners="{Binding !HidePinDecoration}"
                                                   ShowBorder="{Binding !HidePinBorder}"
                                                   SelectionThickness="{Binding SelectionThickness}"
                                                   WingWidth="{Binding WingWidth}"
                                                   WingHeight="{Binding WingHeight}"
                                                   SelectionIconSize="{Binding SelectionIconSize}"
                                                   LeftWingMargin="{Binding LeftWingMargin}"
                                                   RightWingMargin="{Binding RightWingMargin}" />
                    </Grid>

                    <!-- Resize Handles -->
                    <Grid ClipToBounds="False">
                        <Grid Classes="Handle" Name="HandleTopLeft" Cursor="TopLeftCorner" Width="30" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-15"/>
                        <Grid Classes="Handle" Name="HandleTopRight" Cursor="TopRightCorner" Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="-15"/>
                        <Grid Classes="Handle" Name="HandleBottomLeft" Cursor="BottomLeftCorner" Width="30" Height="30" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="-15"/>
                        <Grid Classes="Handle" Name="HandleBottomRight" Cursor="BottomRightCorner" Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="-15"/>
                        <Grid Classes="Handle" Name="HandleTop" Cursor="TopSide" Height="15" HorizontalAlignment="Stretch" VerticalAlignment="Top" Margin="15,0"/>
                        <Grid Classes="Handle" Name="HandleBottom" Cursor="BottomSide" Height="15" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="15,0"/>
                        <Grid Classes="Handle" Name="HandleLeft" Cursor="LeftSide" Width="15" HorizontalAlignment="Left" VerticalAlignment="Stretch" Margin="0,15"/>
                        <Grid Classes="Handle" Name="HandleRight" Cursor="RightSide" Width="15" HorizontalAlignment="Right" VerticalAlignment="Stretch" Margin="0,15"/>
                    </Grid>

                    <!-- Text Input Overlay -->
                    <Canvas IsHitTestVisible="True" ZIndex="5">
                         <controls:TextEntryOverlay DataContext="{Binding}"
                                                    Canvas.Left="{Binding TextInputPosition.X}"
                                                    Canvas.Top="{Binding TextInputPosition.Y}"/>
                    </Canvas>
                </Grid>
            </Border>
        </Border>

        <!-- Bottom Toolbar (Overlay positioned inside window padding) -->
        <Border x:Name="ToolbarBorder" IsVisible="{Binding ShowToolbar}" 
                Margin="{Binding ToolbarMargin}"
                Background="#1A1A1A" BorderBrush="#333333" BorderThickness="1" 
                CornerRadius="4" Padding="6,2"
                HorizontalAlignment="Center" VerticalAlignment="Bottom"
                MinWidth="420"
                BoxShadow="0 4 15 0 #D0000000"
                ZIndex="20">
            <Border.Styles>
                <Style Selector="Border">
                    <Setter Property="Transitions">
                        <Transitions>
                            <DoubleTransition Property="Opacity" Duration="0:0:0.2"/>
                        </Transitions>
                    </Setter>
                </Style>
            </Border.Styles>

            <StackPanel Orientation="Vertical" Spacing="4" HorizontalAlignment="Stretch">
                <!-- Row 1: Drawing Tools -->
                <StackPanel Orientation="Horizontal" Spacing="4" HorizontalAlignment="Center">
                    <controls:DrawingToolbar DataContext="{Binding}"/>
                    <Rectangle Fill="#444444" Width="1" Height="20" Margin="4,0" VerticalAlignment="Center"/>
                    <Button Theme="{StaticResource SnipButton}" Command="{Binding ClearAnnotationsCommand}" ToolTip.Tip="{Binding ClearTooltip}">
                        <PathIcon Data="M19,4h-3.5l-1-1h-5l-1,1H5v2h14V4z M6,19c0,1.1,0.9,2,2,2h8c1.1,0,2-0.9,2-2V7H6V19z" Width="14" Height="14"/>
                    </Button>
                    <Rectangle Fill="#444444" Width="1" Height="20" Margin="4,0" VerticalAlignment="Center"/>
                    <!-- Trim Button -->
                    <Button Theme="{StaticResource SnipButton}" Command="{Binding ToggleTrimCommand}" ToolTip.Tip="裁切時間"
                            Opacity="{Binding IsTrimmingMode, Converter={StaticResource BoolToOpacityConverter}}">
                        <PathIcon Data="M9.64,7.64C9.87,7.14 10,6.59 10,6A4,4 0 0,0 6,2A4,4 0 0,0 2,6A4,4 0 0,0 6,10C6.59,10 7.14,9.87 7.64,9.64L10,12L7.64,14.36C7.14,14.13 6.59,14 6,14A4,4 0 0,0 2,18A4,4 0 0,0 6,22A4,4 0 0,0 10,18C10,17.41 9.87,16.86 9.64,16.36L12,14L19,21H22V20L9.64,7.64M6,8A2,2 0 0,1 4,6A2,2 0 0,1 6,4A2,2 0 0,1 8,6A2,2 0 0,1 6,8M6,20A2,2 0 0,1 4,18A2,2 0 0,1 6,16A2,2 0 0,1 8,18A2,2 0 0,1 6,20M12,12.5A0.5,0.5 0 0,1 11.5,12A0.5,0.5 0 0,1 12,11.5A0.5,0.5 0 0,1 12.5,12A0.5,0.5 0 0,1 12,12.5M19,3L13,9L15,11L22,4V3H19Z" Width="14" Height="14"/>
                    </Button>
                    <!-- Timeline (multi-segment cut) toggle -->
                    <Button Theme="{StaticResource SnipButton}" Command="{Binding ToggleTimelineCommand}" ToolTip.Tip="{Binding TimelineTooltip}"
                            Opacity="{Binding IsTimelineMode, Converter={StaticResource BoolToOpacityConverter}}">
                        <PathIcon Data="M18,4L20,8H17L15,4H13L15,8H12L10,4H8L10,8H7L5,4H4A2,2 0 0,0 2,6V18A2,2 0 0,0 4,20H20A2,2 0 0,0 22,18V4H18Z" Width="14" Height="14"/>
                    </Button>
                    <!-- Split at playhead (timeline mode only) -->
                    <Button Theme="{StaticResource SnipButton}" Command="{Binding SplitSegmentCommand}" ToolTip.Tip="{Binding SplitTooltip}"
                            IsVisible="{Binding IsTimelineMode}">
                        <PathIcon Data="M14,4L18,8L14,12V9H6V7H14V4M10,12L6,16L10,20V17H18V15H10V12Z" Width="14" Height="14"/>
                    </Button>
                    <!-- Delete selected segment (timeline mode only) -->
                    <Button Theme="{StaticResource SnipButton}" Command="{Binding DeleteSegmentCommand}" ToolTip.Tip="{Binding DeleteSegmentTooltip}"
                            IsVisible="{Binding IsTimelineMode}">
                        <PathIcon Data="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z" Width="14" Height="14"/>
                    </Button>
                    <Rectangle Fill="#444444" Width="1" Height="20" Margin="4,0" VerticalAlignment="Center"/>
                    <Button Theme="{StaticResource SnipButton}" Command="{Binding CopyCommand}" ToolTip.Tip="{Binding CopyTooltip}">
                        <PathIcon Data="M19,21H8V7H19M19,5H8A2,2 0 0,0 6,7V21A2,2 0 0,0 8,23H19A2,2 0 0,0 21,21V7A2,2 0 0,0 19,5M16,1H4A2,2 0 0,0 2,3V17H4V3H16V1Z" Width="14" Height="14"/>
                    </Button>
                    <Button Theme="{StaticResource SnipButton}" Command="{Binding SaveCommand}" ToolTip.Tip="{Binding SaveTooltip}">
                        <PathIcon Data="M15,9H5V5H15M12,19A3,3 0 0,1 9,16A3,3 0 0,1 12,13A3,3 0 0,1 15,16A3,3 0 0,1 12,19M17,3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V7L17,3Z" Width="14" Height="14"/>
                    </Button>
                    <Button Theme="{StaticResource SnipButton}" Command="{Binding CloseCommand}" ToolTip.Tip="{Binding CloseTooltip}">
                        <PathIcon Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" Width="14" Height="14"/>
                    </Button>
                </StackPanel>

                <Rectangle Fill="#333333" Height="1" Margin="2,0" HorizontalAlignment="Stretch"/>

                <!-- Row 2: Playback Controls -->
                <DockPanel Margin="4,2" Height="32">
                    <!-- Left: Playback buttons -->
                    <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" Spacing="2" VerticalAlignment="Center">
                        <Button Theme="{StaticResource SnipButton}" Width="30" Height="30" Command="{Binding RewindCommand}" ToolTip.Tip="Rewind 5s">
                            <PathIcon Data="M11,18V6L2.5,12L11,18M18,18V6L9.5,12L18,18Z" Width="12" Height="12"/>
                        </Button>
                        <Button Theme="{StaticResource SnipButton}" Width="30" Height="30" Command="{Binding TogglePlaybackCommand}" ToolTip.Tip="{Binding PlaybackTooltip}">
                            <Panel>
                                <PathIcon Data="M14,19H18V5H14M6,19H10V5H6V19Z" Width="12" Height="12" IsVisible="{Binding IsPlaying}"/>
                                <PathIcon Data="M8,5.14V19.14L19,12.14L8,5.14Z" Width="12" Height="12" IsVisible="{Binding !IsPlaying}"/>
                            </Panel>
                        </Button>
                        <Button Theme="{StaticResource SnipButton}" Width="30" Height="30" Command="{Binding FastForwardCommand}" ToolTip.Tip="Fast Forward 5s">
                            <PathIcon Data="M13,6V18L21.5,12L13,6M6,6V18L14.5,12L6,6Z" Width="12" Height="12"/>
                        </Button>
                        <Button Theme="{StaticResource SnipButton}" Width="30" Height="30" Command="{Binding ToggleLoopCommand}" ToolTip.Tip="{Binding RepeatTooltip}">
                            <PathIcon Data="M17,17H7V14L3,18L7,22V19H19V13H17V17M7,7H17V10L21,6L17,2V5H5V11H7V7Z" Width="12" Height="12" 
                                      Opacity="{Binding IsLooping, Converter={StaticResource BoolToOpacityConverter}}"/>
                        </Button>
                        <Button Theme="{StaticResource SnipButton}" Width="30" Height="30" Command="{Binding ToggleMuteCommand}" ToolTip.Tip="{Binding MuteTooltip}">
                            <Panel>
                                <PathIcon Data="M14,3.23V20.77C14,21.37 13.33,21.74 12.83,21.42L7.6,18H3C1.9,18 1,17.1 1,16V8C1,6.9 1.9,6 3,6H7.6L12.83,2.58C13.33,2.26 14,2.63 14,3.23M19,9L16.41,11.59L19,14.17L17.59,15.59L15,13L12.41,15.59L11,14.17L13.59,11.59L11,9L12.41,7.59L15,10.17L17.59,7.59L19,9Z" Width="12" Height="12" IsVisible="{Binding IsMuted}"/>
                                <PathIcon Data="M14,3.23V20.77C14,21.37 13.33,21.74 12.83,21.42L7.6,18H3C1.9,18 1,17.1 1,16V8C1,6.9 1.9,6 3,6H7.6L12.83,2.58C13.33,2.26 14,2.63 14,3.23M16,7.82V10.73C17.16,11.14 18,12.25 18,13.55C18,14.84 17.16,15.95 16,16.36V19.27C18.89,18.78 21,16.35 21,13.55C21,10.74 18.89,8.31 16,7.82Z" Width="12" Height="12" IsVisible="{Binding !IsMuted}"/>
                            </Panel>
                        </Button>
                    </StackPanel>
                    <!-- Right: Time + Speed -->
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                        <TextBlock Text="{Binding FormattedTime}" VerticalAlignment="Center" Foreground="#E0E0E0" FontSize="10" FontFamily="Consolas"/>
                        <Button Theme="{StaticResource SnipToolButton}" Content="{Binding PlaybackSpeedText}" 
                                Command="{Binding CycleSpeedCommand}" 
                                Padding="4,2" FontSize="11" Width="40" Height="26" VerticalAlignment="Center"/>
                    </StackPanel>
                    <!-- Center: Slider fills remaining space -->
                    <Slider Minimum="0" Maximum="{Binding TotalDuration.TotalSeconds}" 
                            Value="{Binding CurrentTimeSeconds}" 
                            VerticalAlignment="Center" Margin="8,0">
                        <Slider.RenderTransform>
                            <TranslateTransform Y="-10"/>
                        </Slider.RenderTransform>
                    </Slider>
                </DockPanel>

                <!-- Row 3: Trim Panel (inside toolbar, appears when trimming mode is active) -->
                <StackPanel IsVisible="{Binding IsTrimmingMode}" Orientation="Vertical" Spacing="4">
                    <Rectangle Fill="#333333" Height="1" Margin="2,0" HorizontalAlignment="Stretch"/>

                    <!-- Trim Sliders (overlaid to create range slider) -->
                    <Grid x:Name="TrimTrackGrid" Height="28" Margin="4,0">
                        <!-- Track background -->
                        <Border Background="#333333" CornerRadius="2" Height="6" VerticalAlignment="Center" Margin="8,0"/>
                        
                        <!-- Selected range highlight -->
                        <Border Background="#555555" CornerRadius="2" Height="6" VerticalAlignment="Center" Margin="8,0"
                                HorizontalAlignment="Stretch" IsHitTestVisible="False"/>

                        <!-- Start Thumb (custom Thumb control, positioned by code-behind) -->
                        <Thumb x:Name="TrimStartThumb" Width="12" Height="22" 
                               HorizontalAlignment="Left" VerticalAlignment="Center"
                               Cursor="SizeWestEast">
                            <Thumb.Template>
                                <ControlTemplate>
                                    <Border Background="#FF4444" CornerRadius="2" Width="12" Height="22"/>
                                </ControlTemplate>
                            </Thumb.Template>
                        </Thumb>

                        <!-- End Thumb (custom Thumb control, positioned by code-behind) -->
                        <Thumb x:Name="TrimEndThumb" Width="12" Height="22" 
                               HorizontalAlignment="Left" VerticalAlignment="Center"
                               Cursor="SizeWestEast">
                            <Thumb.Template>
                                <ControlTemplate>
                                    <Border Background="#4488FF" CornerRadius="2" Width="12" Height="22"/>
                                </ControlTemplate>
                            </Thumb.Template>
                        </Thumb>
                    </Grid>

                    <!-- Time Input Panel -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8" Margin="0,0,0,2">
                        <!-- Start Time -->
                        <StackPanel Orientation="Horizontal" Spacing="2" VerticalAlignment="Center">
                            <TextBlock Text="起始" Foreground="#FF4444" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <controls:CompactNumericStep Value="{Binding TrimStartMinutes}" 
                                                         IncreaseCommand="{Binding IncTrimStartMinCmd}" 
                                                         DecreaseCommand="{Binding DecTrimStartMinCmd}"/>
                            <TextBlock Text=":" Foreground="#AAAAAA" FontSize="11" VerticalAlignment="Center" FontWeight="Bold"/>
                            <controls:CompactNumericStep Value="{Binding TrimStartSecs}" 
                                                         IncreaseCommand="{Binding IncTrimStartSecCmd}" 
                                                         DecreaseCommand="{Binding DecTrimStartSecCmd}"/>
                        </StackPanel>

                        <Rectangle Fill="#444444" Width="1" Height="18" VerticalAlignment="Center"/>

                        <!-- End Time -->
                        <StackPanel Orientation="Horizontal" Spacing="2" VerticalAlignment="Center">
                            <TextBlock Text="終點" Foreground="#4488FF" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <controls:CompactNumericStep Value="{Binding TrimEndMinutes}" 
                                                         IncreaseCommand="{Binding IncTrimEndMinCmd}" 
                                                         DecreaseCommand="{Binding DecTrimEndMinCmd}"/>
                            <TextBlock Text=":" Foreground="#AAAAAA" FontSize="11" VerticalAlignment="Center" FontWeight="Bold"/>
                            <controls:CompactNumericStep Value="{Binding TrimEndSecs}" 
                                                         IncreaseCommand="{Binding IncTrimEndSecCmd}" 
                                                         DecreaseCommand="{Binding DecTrimEndSecCmd}"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <!-- Row 4: Proportional timeline strip — blocks sized by length; click to select, then
                     Delete; Split cuts at the playhead. Pixel layout + playhead are set in code-behind. -->
                <StackPanel IsVisible="{Binding IsTimelineMode}" Orientation="Vertical" Spacing="3">
                    <Rectangle Fill="#333333" Height="1" Margin="2,0" HorizontalAlignment="Stretch"/>
                    <Grid x:Name="SegmentStripGrid" Height="30" Margin="4,0">
                        <!-- Track background -->
                        <Border Background="#262626" CornerRadius="3"/>
                        <!-- Kept-segment blocks, positioned proportionally on a canvas -->
                        <ItemsControl ItemsSource="{Binding SegmentBlocks}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.Styles>
                                <Style Selector="ContentPresenter" x:DataType="vm:SegmentBlockViewModel">
                                    <Setter Property="Canvas.Left" Value="{Binding PixelLeft}"/>
                                    <Setter Property="Canvas.Top" Value="0"/>
                                </Style>
                            </ItemsControl.Styles>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="vm:SegmentBlockViewModel">
                                    <Panel Width="{Binding PixelWidth}" Height="30">
                                        <Button Theme="{StaticResource SnipToolButton}"
                                                Width="{Binding PixelWidth}" Height="30"
                                                Padding="2,0" FontSize="9"
                                                Content="{Binding Label}"
                                                Command="{Binding $parent[Window].((vm:FloatingVideoViewModel)DataContext).SelectSegmentCommand}"
                                                CommandParameter="{Binding Index}"/>
                                        <!-- Selected-segment outline -->
                                        <Border IsVisible="{Binding IsSelected}" BorderBrush="#FFD400"
                                                BorderThickness="2" CornerRadius="3" IsHitTestVisible="False"/>
                                    </Panel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <!-- Playhead (X set in code-behind via RenderTransform) -->
                        <Rectangle x:Name="SegmentPlayhead" Width="2" Fill="#FF4444"
                                   HorizontalAlignment="Left" VerticalAlignment="Stretch" IsHitTestVisible="False"/>
                    </Grid>
                    <TextBlock Text="{Binding TimelineHint}" Foreground="#999999" FontSize="9"
                               HorizontalAlignment="Center" Margin="0,0,0,2"/>
                </StackPanel>
            </StackPanel>
        </Border>

         <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="{Binding [MenuCopyVideo], Source={x:Static services:LocalizationService.Instance}}" Command="{Binding CopyCommand}"/>
                <MenuItem Header="{Binding [MenuSaveVideo], Source={x:Static services:LocalizationService.Instance}}" Command="{Binding SaveCommand}"/>
                <Separator/>
                <MenuItem Header="{Binding [MenuShowToolbar], Source={x:Static services:LocalizationService.Instance}}" 
                          Command="{Binding ToggleToolbarCommand}">
                    <MenuItem.Icon>
                        <CheckBox IsChecked="{Binding ShowToolbar}" IsHitTestVisible="False"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="{Binding [MenuClose], Source={x:Static services:LocalizationService.Instance}}" Command="{Binding CloseCommand}"/>
            </ContextMenu>
        </Grid.ContextMenu>


    </Grid>
</Window>


